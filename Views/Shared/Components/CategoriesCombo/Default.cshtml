@model List<EscolaDeVoce.Services.ViewModel.CategoryViewModel>

@{
    var id = ViewBag.id;
}
<select name="categoriesId" multiple id="categoriesId">
    @foreach(var item in Model){
        <option value="@item.Id">@item.name</option>
    }
</select>